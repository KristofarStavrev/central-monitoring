global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'fastapi_app'
    metrics_path: /metrics
    basic_auth:
      username: "${PROMETHEUS_AUTH_UNAME}"
      password: "${PROMETHEUS_AUTH_PASS}"
    static_configs:
      - targets: ["${PROMETHEUS_SCRAPE_TARGET}"]
